export function Game({ gameId }: { gameId: string }) {
  return (
    <main>
      <h1>Game {gameId}</h1>
      <p>Scaffold placeholder — see issue #7 for the scorecard screen.</p>
    </main>
  );
}
